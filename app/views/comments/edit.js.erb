if ($("#add_comment_form_<%= @answer.id %>").length == 0) {
  $("#edit_comment_<%= @comment.id %>").hide('slow', function() {
    $(this).after("<%=j render 'form' %>");
  });
} else {
  $("#add_comment_form_<%= @answer.id %>").html("<%=j render 'form' %>");
}