<%= form_for([@question, @answer]) do |f| %>
  <div class="form-group">
    <%= f.label :body, class: 'control-label' %>
    <%= f.text_field :body, class: 'form-control', autofocus: true, placeholder: 'Дать ответ' %>
  </div>

  <div class="form-group">
    <%= f.submit 'Дать ответ', class: 'btn btn-info' %>
  </div>
<% end %>